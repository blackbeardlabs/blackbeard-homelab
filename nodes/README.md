# Nodes

Node files use the naming convention `node-XX-name-v1.md`.

When hardware changes materially, create a new versioned file (`v2`, `v3`, ...) instead of overwriting the previous one. This preserves historical benchmark context.

Benchmarks should always reference an exact node version.
